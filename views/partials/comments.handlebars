{{#if singleResume.comments}}
<section class="card my-5">
      <div class="card-header" id="card-header">
        <h3 class="m-3">Comments</h3>
      </div>

      <div class="card-body m-3">
        {{#each singleResume.comments as |comments| }}
          <div>
            <p>
              {{comments.content}}
            </p>
            <p>
              &mdash; {{user.username}}, {{format_date createdAt}}
            </p>
          </div>
        {{/each}}
      </div>
    </section>
  {{/if}}