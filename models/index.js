const User = require('./User');
// const Resume = require('./Resume')
// const Comment = require('./Comment')

// Associations
// User.hasMany(Resume, {
//     foreignKey: 'user_id'
// })

// User.hasMany(Comment, {
//     foreignKey: 'user_id'
// })

// Resume.belongsTo(User, {
//     foreignKey: 'user_id'
// })

// Resume.hasMany(Comment, {
//     foreignKey: 'resume_id'
// })

// Comment.belongsTo(User, {
//     foreignKey: 'user_id'
// })

// Comment.belongsTo(Resume, {
//     foreignKey: 'resume_id'
// })

module.exports = {
    User,
    // Resume,
    // Comment
};
